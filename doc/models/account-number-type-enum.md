
# Account Number Type Enum

One of `iban`, `clabe`, `wallet_address`, or `other`. Use `other` if the bank account number is in a generic format.

## Enumeration

`AccountNumberTypeEnum`

## Fields

| Name |
|  --- |
| `CLABE` |
| `IBAN` |
| `OTHER` |
| `PAN` |
| `WALLET_ADDRESS` |

