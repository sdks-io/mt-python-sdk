
# Ledger Type Enum

An optional type to auto-sync the counterparty to your ledger. Either `customer` or `vendor`.

## Enumeration

`LedgerTypeEnum`

## Fields

| Name |
|  --- |
| `CUSTOMER` |
| `VENDOR` |

