"""moderntreasury.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501

class ReturnableTypeEnum(object):
    """Implementation of the 'ReturnableType' enum.

    The type of object being returned or `null`.

    Attributes:
        INCOMING_PAYMENT_DETAIL: The enum member of type str.
        PAPER_ITEM: The enum member of type str.
        PAYMENT_ORDER: The enum member of type str.
        RETURN: The enum member of type str.
        REVERSAL: The enum member of type str.

    """

    _all_values = ["incoming_payment_detail", "paper_item", "payment_order",
        "return", "reversal"]
    INCOMING_PAYMENT_DETAIL = "incoming_payment_detail"

    PAPER_ITEM = "paper_item"

    PAYMENT_ORDER = "payment_order"

    ENUM_RETURN = "return"

    REVERSAL = "reversal"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
