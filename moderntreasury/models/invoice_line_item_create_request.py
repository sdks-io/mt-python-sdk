"""moderntreasury.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from moderntreasury.api_helper import APIHelper


class InvoiceLineItemCreateRequest(object):
    """Implementation of the 'invoice_line_item_create_request' model.

    Attributes:
        name (str): The name of the line item, typically a product or SKU name.
        description (str): An optional free-form description of the line item.
        quantity (int): The number of units of a product or service that this line
            item is for.           Must be a whole number. Defaults to 1 if not
            provided.
        unit_amount (int): The cost per unit of the product or service that this line
            item is for,           specified in the invoice currency's smallest unit.
        direction (str): Either `debit` or `credit`. `debit` indicates that a client
            owes the business money           and increases the invoice's
            `total_amount` due. `credit` has the opposite intention and effect.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": "name",
        "unit_amount": "unit_amount",
        "description": "description",
        "quantity": "quantity",
        "direction": "direction",
    }

    _optionals = [
        "description",
        "quantity",
        "direction",
    ]

    def __init__(
        self,
        name=None,
        unit_amount=None,
        description=APIHelper.SKIP,
        quantity=APIHelper.SKIP,
        direction=APIHelper.SKIP):
        """Initialize a InvoiceLineItemCreateRequest instance."""
        # Initialize members of the class
        self.name = name
        if description is not APIHelper.SKIP:
            self.description = description
        if quantity is not APIHelper.SKIP:
            self.quantity = quantity
        self.unit_amount = unit_amount
        if direction is not APIHelper.SKIP:
            self.direction = direction

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else None
        unit_amount =\
            dictionary.get("unit_amount")\
            if dictionary.get("unit_amount")\
                else None
        description =\
            dictionary.get("description")\
            if dictionary.get("description")\
                else APIHelper.SKIP
        quantity =\
            dictionary.get("quantity")\
            if dictionary.get("quantity")\
                else APIHelper.SKIP
        direction =\
            dictionary.get("direction")\
            if dictionary.get("direction")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(name,
                   unit_amount,
                   description,
                   quantity,
                   direction)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _name=self.name
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _quantity=(
            self.quantity
            if hasattr(self, "quantity")
            else None
        )
        _unit_amount=self.unit_amount
        _direction=(
            self.direction
            if hasattr(self, "direction")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"name={_name!r}, "
            f"description={_description!r}, "
            f"quantity={_quantity!r}, "
            f"unit_amount={_unit_amount!r}, "
            f"direction={_direction!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _name=self.name
        _description=(
            self.description
            if hasattr(self, "description")
            else None
        )
        _quantity=(
            self.quantity
            if hasattr(self, "quantity")
            else None
        )
        _unit_amount=self.unit_amount
        _direction=(
            self.direction
            if hasattr(self, "direction")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"name={_name!s}, "
            f"description={_description!s}, "
            f"quantity={_quantity!s}, "
            f"unit_amount={_unit_amount!s}, "
            f"direction={_direction!s}, "
            f")"
        )
