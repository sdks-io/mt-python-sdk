"""moderntreasury.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501

class RoutingNumberType7(object):
    """Implementation of the 'RoutingNumberType7' enum.

    One of `aba`, `au_bsb`, `br_codigo`, `ca_cpa`, `cnaps`, `gb_sort_code`,
    `in_ifsc`, `my_branch_code`, or `swift`. In sandbox mode we currently only
    support `aba` and `swift` with routing numbers '123456789' and 'GRINUST0XXX'
    respectively.

    Attributes:
        ABA: The enum member of type str.
        AU_BSB: The enum member of type str.
        CA_CPA: The enum member of type str.
        GB_SORT_CODE: The enum member of type str.
        IN_IFSC: The enum member of type str.
        SWIFT: The enum member of type str.

    """

    ABA = "aba"

    AU_BSB = "au_bsb"

    CA_CPA = "ca_cpa"

    GB_SORT_CODE = "gb_sort_code"

    IN_IFSC = "in_ifsc"

    SWIFT = "swift"

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
